// Mock data removed. Live API data should be used instead.

export const mockUsers = [];
export const mockCourses = [];
export const mockAssignments = [];
export const mockQuizzes = [];
export const mockNotifications = [];
export const mockAnnouncements = [];
export const mockSubmissions = [];
export const mockMessages = [];

export const mockLearnerStats = {
    enrolledCourses: 0,
    completedCourses: 0,
    hoursLearned: 0,
    currentStreak: 0,
    certificates: 0,
    avgScore: 0,
    weeklyHours: [],
    weeklyLabels: [],
};

export const mockInstructorStats = {
    totalCourses: 0,
    totalLearners: 0,
    pendingGrading: 0,
    avgRating: 0,
    completionRate: 0,
    recentActivity: [],
};

export const mockAdminStats = {
    totalUsers: 0,
    activeLearners: 0,
    activeInstructors: 0,
    totalCourses: 0,
    completionRate: 0,
    monthlyActiveUsers: 0,
    newUsersThisWeek: 0,
    systemAlerts: [],
};

export const mockSkills = [];

export const mockDailyGoal = {
    completed: 0,
    target: 0,
    percentage: 0,
    message: '',
};
